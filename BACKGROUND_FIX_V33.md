# HOHI DRIVE V33

- Enables continuous Core Location background navigation (`allowsBackgroundLocationUpdates = true`).
- Adds 2-second HUD heartbeat while navigation is active.
- Adds explicit `nav_active` state to HUD packets.
- Adds Order > Test QR on HUD so payment QR can be verified without completing a real trip.
- Pair with ESP32 HUD firmware V9.3.
